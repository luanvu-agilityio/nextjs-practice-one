// Breadcrumb-focused tests were consolidated into index.test.tsx to avoid mock hoisting
// conflicts between multiple test modules. Keep this file inert so jest won't run
// duplicate mocks across test files.

test.todo('Breadcrumb tests consolidated into index.test.tsx');
